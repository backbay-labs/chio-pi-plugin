# Chio Pi integration

This repository owns the Pi host adapter. Use the native Pi extension and SDK contracts. Do not execute protected effects locally after an authorization precheck. Keep stock-host contract tests separate from real kernel acceptance. Never change the user's normal Pi profile or print credentials. Use conventional commits. No em dashes.
