# Bounded real-host authority observations

No host is accepted by this record. Each successful case used the actual native host, actual OpenAI provider, current kernel, and independent readonly Docker resource and dispatch observations. Required I01-I08 cases remain open.

The aggregate budget run authorized three invocations shared across four tools. Write, edit, and read completed and were acknowledged; list received a signed kernel invocation-budget denial. Exactly three resource dispatches occurred.

The seven approval stages cover pending, missing decision, changed arguments, approved resume, completed replay, pending rejection, and rejected resume. Only approved resume performed a resource effect. Native host tool-call identities and arguments are retained in each native-dispatch.json. Additional model attempts are retained and included in all resource observations. Operator approval itself caused no protected dispatch.

The earlier approval harness rejected three model attempts because it demanded exactly one. All three had no effect. The revised check requires the intended call and retains all extra attempts while still asserting every resource effect. The r2 candidate also revalidates changed complete native-history outcomes even when a prior request identifier was already acknowledged.
