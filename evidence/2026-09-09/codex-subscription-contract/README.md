This is a failed rerun against the previous artifact. npm failed to replace the same-version bundled package and the old installed code remained. The driver was mistakenly started before checking installation status. It reproduced the original encrypted-reasoning content-array refusal after one authorized write. Both the failure and explicit operator recovery without protected redispatch are retained. This directory is not evidence for the fixed candidate.
