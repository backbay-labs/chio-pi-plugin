# Received-result qualification

Status: unaccepted. The predecessor actual Pi/OpenAI session accepted injected
forged output under an unchanged authentic kernel receipt and acknowledged it.
The repaired HTTP adapter checks the expected host operation identity and hashes
received bytes against the signed terminal result before acknowledgement.

The cold-installed artifact now returns unresolved under the same injected
fault. It exposes no forged text as a result, retains one original read, and
does not acknowledge delivery. Independent resource observations show unchanged
file bytes and exactly one read dispatch. No new authority replaces that
unresolved operation. Component tests cover valid delivery, substituted bytes,
a valid receipt for another operation, and an invalid signature; 18 tests pass
without skips across the suite.

The same installed artifact passed useful write/edit/read/list with four
confirmed deliveries, forbidden read and write with no dispatches, and final
HTTP response loss with same-session resume fencing and exact operator recovery.
The resource and operator journals remain outside the guest. The injected
fault is a test-only parent preload and is absent from the installed package.

These five bounded real-host cases do not close all I01-I08 gates. Full authority,
approvals, budget exhaustion, revocation, kernel/plugin faults, cancellation,
later crash cutpoints, lifecycle procedures and publication remain required.
Artifact identities and original failing transcripts are retained alongside
the repaired runs. Source and packaged implementation hashes remain explicit.
