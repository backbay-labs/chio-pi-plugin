# Final candidate preparation

These are local archive lifecycle observations, not real-kernel acceptance or
publication. No kernel ran in these commands. Confidence in the recorded
installation and removal observations is high. Final kernel testing remains open.

The exact original files are retained as deterministic gzip objects. `files.json`
binds every object to its original byte hash and source path. Only evidence
folders were exported; no npm cache, consumer installation, normal profile,
provider credential or private kernel authority was copied.

The unchanged `ec6095390b9eae233540b73aee0ad2fef6977c36122dd30aa2779329b1897aa1`
archive uses the documented public `@earendil-works/pi-coding-agent@0.85.1`
peer and nested install contract. Attempt 3 passes retained candidate upgrade,
current help, removal of the dedicated installation, public peer reinstallation,
current archive reinstallation and reinstalled help. All 1,090 regular archive
files match installed bytes. Predecessor archive:
`78257af62fc840d8806dd9d90f124ff8c0a2a9b63f3588a4adb6bee4a44a6960`.

Attempts 1 and 2 are preserved. Attempt 1's extra offline empty-cache plugin
installation failed resolving public peer metadata. Attempt 2 successfully
installed and upgraded using the public registry, then its extra offline npm
uninstall failed because that fresh cache had no peer metadata. Neither is the
published removal procedure. Attempt 3 follows the archive README: remove only
the dedicated inactive installation, then reinstall the public peer and archive.
No force or legacy-peer dependency exception is used. Offline standalone Pi
installation and offline npm-uninstall are not claimed.

`qualify-kernel-storage.py` now accepts explicit case-name prefixes and three
unique ports, preserving the old defaults. Invalid duplicate ports refuse before
creating output. Final storage runs will use fresh owners and all three actual
SQLite cutpoints; those results are pending.
